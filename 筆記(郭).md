## 第一課：開始本地端的 git 版本控制
* **第一步**：`git init`，對資料夾初始化，讓普通的資料夾成為有版控的 git 專案。只要刪除 `.git` 資料夾，就會退回無版控狀態。
* **第二步**：`git add main.py`添加單一檔案，或「git add .」把所有檔案加進去。這一步是用來將檔案提交到暫存區。
* **第三步**：`git commit -m "訊息"`，將狀態保存成快照並收進 `.git` 中。

## 第二課：防止機密外洩 (.gitignore)
* **第一步**：建立 `.gitignore` 檔案，將機密資料從未來快照的名單中排除。
* **第二步**：此時再用`git add .`，就會根據此檔案避開特定內容。
* **特殊情況**：
    1. 假設已將不對的檔案在寫好 `.gitignore` 前就提交過，該如何從 git 紀錄中刪除？
       * **Ans.** `git rm --cached .env`。rm 是 remove 的意思，如果沒加上 `--cached`參數會直接從目錄刪除檔案，而非只從記錄快取中移除。
    2. `git rm --cached .env`和 `.gitignore` 的差別？
       * **Ans.** `.gitignore` 是守衛的入場黑名單，用於阻止尚未進入的，避免加入追蹤名單。而`git rm --cached`是將場內的趕出去，也就是已經被追蹤過的從名單中移除。
    3. 如果機密資料已經 commit 過，且甚至 push 上了 github 等，怎麼應對？
       * **Ans.** 
         1. 最高指導原則：寫下第一行 Code 之前，就先建好 `.gitignore`。
         2. 如果不小心 commit 但還沒 push：這時候只有在本地，用 AI 執行歷史抹除指令（重寫本地的歷史快照）。
         3. 如果已經 push 上去了：馬上作廢 API Key 等資訊並重新申請，因為舊的可能被爬蟲洩漏。

## 第三課：三種反悔方式
* **第一種：Discard（放棄治療）**
    * **時機**：改了一堆扣發現全爛了，但「還沒」執行 `git add` 和 `git commit`。
    * **指令**：`git restore .`（或是直接在 VS Code 原始碼控制介面按復原按鈕）。
    * **AI 提示詞**：「幫我把這個資料夾裡所有『還沒存檔』的修改全部拋棄，恢復到上一次 commit 的乾淨狀態。」
    * **底層思路**：把整張圖紙扔進垃圾桶，拿出上一版已經存檔好的底稿。*(補充：注意，丟進垃圾桶的未存檔草稿是找不回來的！)*
* **第二種：Reset（偷偷重來）**
    * **時機**：不僅改爛了還手殘執行了 commit（存進本地相簿了），但還沒 push 到 GitHub 上。
    * **指令**：`git reset --hard HEAD~1`（或是指定某個 commit ID）。
    * **AI 提示詞**：「我剛剛的 commit 裡有 Bug，且我還沒 push。請幫我把整個倉庫強制退回到『上一次的 commit』，並把剛才那個錯誤的紀錄徹底從歷史中刪除。」
    * **底層思路**：Reset 會強制拉回過去的某個時間點，並且把那個時間點之後發生的所有未來，全部抹煞掉。這招會「篡改歷史」，只能在本地端使用。如果你的歷史已經跟組員共享了你還硬 Reset，同事的 Git 宇宙就會跟你的產生撕裂。
* **第三種：Revert（光明正大地反悔）**
    * **時機**：不只 commit 了還 push 到 GitHub 上，甚至組員都已經下載了。
    * **指令**：`git revert <錯的那個 Commit ID>`
    * **AI 提示詞**：「我之前推送到 GitHub 的那個 commit (ID: 1a2b3c4) 發現有嚴重錯誤。請幫我產生一個 Revert 提交來抵銷它，確保不要破壞既有的歷史紀錄。」
    * **底層思路**：既然歷史已經公開，就不能像 Reset 偷偷塗改。Revert 會去分析你那個寫爛的 commit 到底加了什麼、刪了什麼，然後自動幫你生出一個「完全相反」的全新 commit。例如：之前的 commit 是「加了三行爛 Code」，Revert 就會生一個新的 commit，內容是「刪除那三行爛 Code」。好處是歷史紀錄會誠實地保留，是多人協作時最安全的反悔方式。

## 第四課：平行宇宙的創造和合併
* **第一步：創造並進入分支 (Branch)**
    * **指令**：`git branch 分支名字`(創造)+`git checkout 分支名字`(切換)。*(補充：用 `git checkout -b 分支名字` 可以一行搞定！)*
    * **AI 提示詞**：「我要開發***功能了，請幫我基於目前的進度，建立並切換到一個名為***的新分支。」
    * **底層思路**：你下的指令，就是告訴 Git：「嘿，幫我在這張最新的快照上，貼上一張名叫***的標籤，然後把我的『現在位置（HEAD）』移到這張標籤上。」
* **第二步：合併 (Merge)**
    * **指令**：
        1. 先切換回主幹：`git checkout main`
        2. 執行合併：`git merge 分支名字`
    * **AI 提示詞**：「我在***分支的開發已經完成了。幫我切回 main 分支，並把***的成果合併進來。」
    * **底層思路**：先切回到目的地，再把支線拉過來。
    * **注意**：做任何修改前務必確認 Head 的所在位置，可用`git status`或直接在編輯器內按鈕操作，因為修改只會作用於當前位置。

## 第五課：合併衝突（Merge Conflict）
* **情境**：此時你為了同時做兩個功能，切出兩個分支 A 和 B，現在你將 A 合併回主幹後想要再合併 B，但兩個分支在某些相同段落上有不同寫法。
* **[正常的手動流程]**
    * **AI 提示詞 (遇到衝突時)**：「我剛剛在合併分支時遇到了衝突 (Merge Conflict)，請教我如何在 VS Code 中看懂衝突標記，並告訴我解決衝突的標準流程。」
    * **第一步**：打開有衝突的檔案。
    * **第二步**：在 VS Code 會把撞車的地方高亮標出來，並給幾個選項：
        1. Accept Current Change (保留當前變更)：保留 main 原本的樣子（A）。
        2. Accept Incoming Change (保留傳入變更)：保留準備合併進來的分支的樣子（B）。
        3. Accept Both Changes (兩者都保留)：兩行都留著，要自己手動再修一下。
    * **第三步**：清理檔案中 git 像亂碼的衝突標記（<<<<<<<, =======, >>>>>>>）。
    * **第四步 (重要)**：
        * `git add main.py`（告訴 Git 這個衝突我搞定了）。
        * `git commit -m "解決合併衝突"`（提交合併快照）。
* **[懶人效率流]**
    * **AI 提示詞**：「這段發生衝突了，我的目標是 XXXXX，請幫我解衝突並給出正確的最終代碼。」

## 第六課：雲端協作的 4 大分區與資料流
* **Git 的四大分區**：在雲端協作中，程式碼的狀態分為四個區域。
    1. **遠端庫 (Remote Repository)**： 就是放在 GitHub 上的那個專案庫。大家共用的總部。
    2. **本地庫 (Local Repository)**： 電腦裡那個隱藏的 `.git` 資料夾。
    3. **暫存區 (Staging Area)**： `git add` 後的快照檔案入選名單。
    4. **工作區 (Working Directory)**：在 VS Code 實際正在編輯的那些實體檔案。
* **四大區域的資料搬運方式**：
    * **第一步：把總部資料搬回家 (clone)**
        * **情境**：把 GitHub 專案打包到電腦的資料夾裡。
        * **指令**：`git clone <GitHub網址>`
        * **AI 提示詞**：「幫我從這個 GitHub 網址把專案 clone 下來放到我目前的資料夾。」
        * **底層思路**：clone 下來的專案已經包含 `.git` 資料夾，無須再起始化。
    * **第二步：在分支修改**
        * **注意**：不要直接在 main 線上修改，一律先開分支！
        1. 進入專案
        2. 建立並切到專案分支：`git checkout -b 分支名字`，或是按下 [Create new branch...]
        3. 在工作區用 VS Code 修改程式
        4. 上暫存區：`git add .`
        5. 拍快照：`git commit -m "修改內容"`(此時修改仍只存在於本地)
    * **第三步：將成果推向雲端 (push)**
        * **指令**：`git push origin 當前分支名稱` *(補充：第一次推可以加上 `-u` 參數，變成 `git push -u origin 分支名稱`)*
        * **UI 操作**：VS Code [Publish Branch]
        * **AI 提示詞**：「請幫我把目前分支推送到雲端的 GitHub 上。」
        * **底層思路**：origin 是一個 Git 預設的代名詞，代表「原本的那個遠端總部（GitHub）」。推上去後就能在 GitHub 上看到多出的新分支。
        * **注意**：在推送前應該先確認自己的 main 支線版本已經為最新。多人共編時可能發生交叉推送，也就是在你推送前主線又被更新過，會導致版本與你本地的不同。
    * **第四步：更新本地的主幹，確保後續開發的版本同步**
        * **指令**：`git pull origin main`
        * **注意**：此步驟特別重要，在任何開發前都應該先確定自己的版本與雲端的同步，才能減少合併衝突。

## 第七課：Fork 與 Pull Request (PR)
* **情境**：當你想改寫其他人的倉庫，但你不是管理員無法直接推送，必須先通知對方並等待核准。
* **流程**：複製整個專案 ➡️ 在自己家改 ➡️ 寫信請他收下。
* **第一步**：Fork 一份到自己名下，等同複製整份專案到自己的帳號。
* **第二步**：如同在自己的倉庫一樣的修改和推送。
* **第三步：遞交合併請求 (Pull Request, 簡稱 PR)**
    * **情境**：當你在自己的倉庫修改好後，要向原專案提出合併請求。
    * **操作**：找到 GitHub 上的 [Compare & pull request] 按鈕，並寫下訊息。標題 (做了甚麼)、內容 (為何修改，如何測試)。接著按下 [Create pull request]。

## 第八課：Stash (暫存)
* **情境**：分支寫到一半需要先切換到其他支線修改，但還沒寫完直接提交會導致快照紀錄的是還沒改好的，這時就需要 Stash。
* **VS Code UI 操作**：
    1. 點擊 Source Control 面板上的三個小點 [...]。
    2. 找到 Stash 選單，點擊 [Stash (Include Untracked)]。
    3. VS Code 會要你輸入一個紀錄用的訊息。
    4. 工作區變乾淨了，寫到一半的扣暫時消失，資料夾回到上次提交的狀態。
* **流程**：切換到要修改的支線 ➡️ 開分支修改 ➡️ 提交 ➡️ 切回改到一半的支線
* **切回方式**：點擊 [...] ➡️ Stash ➡️ [Pop Stash...] ➡️ 選擇先前暫存的。
* **底層思路**：Stash 不屬於任何歷史快照，就是一個臨時置物櫃。

## 第九課：Cherry-pick
* **情境**：你和組員同時開發不同支線 (A、B)，此時你需要借用 B 的某個 Commit 的內容，但又不需要合併整個 B 到 A，因為你不要 B 的所有修改都被合過來。
* **指令**：`git cherry-pick abc1234`
* **AI 提示詞**：「我現在在 A 分支。我只需要同事在 B 的某一個特定的 commit（ID: abc1234）。請幫我用 cherry-pick 把那個 commit 單獨抓過來。」
* **底層思路**：Cherry-pick 就是從一堆 Commit 中精準挑出你要的其中一次提交，然後只和這個提交的內容合併，而非完整的兩條支線合併。

## 第十課：竄改歷史（Rebase、Squash 與 Push -f）
* **注意**：此部分的操作須謹慎使用，沒弄好會出事。
* **情境**：
    * 專案的主幹（main）是大家共同的進度，也就是「目前會動的版本」。
    * 你自己開了一個分支叫 feature-cart，負責寫購物車功能。這條分支是你從 main 切出來的。
    * 這幾天裡你 Commit 了很多次才終於完成這個功能，現在你要合併回雲端上的主線，但遇到以下麻煩：
        1. **版本脫節**：組員在你之前先提交了其他分支 (假設是會員登入功能) 並合併了，所以你的本地版本是沒有會員的舊版本。
        2. **本地歷史太雜亂**：由於你完成這條購物車分支不是在一次 Commit 就完成，所以每個快照都只是一小部分的修改，而不是一次簡潔的「購物車完成」這樣的快照。
* **解決方式**：
    * **第一步：打包歷史 (Squash)**
        * 目標是把那幾個零碎的 Commit，整合成 1 個乾淨的紀錄。
        * **VS Code 操作**：
            1. 打開 Git Graph（VS Code 的插件）。
            2. 找到你這幾個 Commit 的前一層（也就是你當初剛切出分支的那個乾淨時間點），按右鍵，選 [Rebase current branch on to this Commit]。
            3. 勾選 [Launch Interactive Rebase (互動式變基)]。
            4. 畫面會跳出你那幾個 Commit。除了時間上最前面那個不動，把其他幾個選項都改成 Squash（意思就是：把它們擠壓、揉進最前面那個 Commit 裡）。*(補充：這邊的「最前面」指的是最上方、時間最早的那個初始 Commit，保留它為 Pick 狀態。)*
            5. 確定後，系統會叫你重新寫一個提交訊息，到這裡就修改完歷史。
    * **第二步：對齊最新進度 (Rebase 變基)**
        * 現在你的歷史紀錄乾淨了，但分支的地基還是舊版，要先把它移植到最新的 main 上。
        1. **更新主幹**：先切回 main，按 Sync (或 Pull)，把最新的會員功能抓到你的電腦裡。
        2. **切回自己分支**：切回你的 feature-cart。
        3. **移植**：打開命令列輸入`git rebase main`（或者在 Git Graph 對著最新的 main 按右鍵選 Rebase）。
        * **底層思路**：Git 會把你先前打包的「購物車 Commit」整個拔起來，直接貼在「會員登入 Commit」後面，你的進度就跟上最新版了。
        * **注意**：Rebase 也會發生類似合併衝突！
        * **解法**：移植實際上是把整條分支的每個提交一個個接到最新的 main 上，每次提交有衝突就要解。但在移植前先打包所有歷史的話就可以減少解衝突次數。
            1. 下達`git rebase main`後遇到衝突，解完後儲存。
            2. 下達`git rebase --continue`繼續執行移植操作。
            3. 假設解衝突的過程不順利，可以直接中止 Rebase，一切會退回開始之前。`git rebase --abort`。
    * **第三步：強制覆蓋雲端 (Push -f)**
        * **注意**：這步驟最危險！另外覆蓋的對象只可是自己正在修改的分支，不可以覆蓋其他支線。
        * 先前的兩步驟都只是在本地修改歷史和對齊，但雲端上的歷史快照還沒更新，直接 push 會被擋下因為紀錄對不上。
        * **VS Code 操作**：點擊 [Source Control] 的 [...] ➡️ 找到 [Branch] ➡️ 點選 [Push (Force)]。
        * **底層思路**：等同強硬地告訴 GitHub：「不要管你上面存了什麼舊資料，以我現在電腦裡這份整理過的新紀錄為準，直接蓋過去！」
* **總整理**：
    * **為何這三個操作要謹慎使用？** 因為假如你對著別人的支線或是主線修改歷史，會導致其他人本地的歷史版本和雲端對不上，會讓他們無法合併新的內容。
    * **結論**：修改歷史的操作都只可以用在自己的分支上，而且是合併上雲端前在本地先修改完，最後才強制覆蓋雲端上的自己那條分支。而面對共用的 main 主線，都用合併 (Merge)，不要亂修改！